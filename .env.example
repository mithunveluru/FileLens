# Copy to `.env.local` (git-ignored) to override defaults during development.
# Only variables prefixed with VITE_ are exposed to the frontend. See
# src/shared/config/env.ts for the typed accessors.

# Minimum frontend log level: debug | info | warn | error
# Defaults to "debug" in dev and "info" otherwise.
VITE_LOG_LEVEL=debug
